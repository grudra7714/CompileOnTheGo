#include <iostream>

using namespace std;

void isValidLogon(char *);


int main(){
	char logonName[80];

	cout << "Enter logon name: ";
	cin.getline(logonName, 80);

	isValidLogon(logonName);

	return 0;
}


void isValidLogon(char *str){
	int i,j,k;
	cout << "String is: " << str[1] << "\n";

	int count = 0, count1 = 0, flag = 1, flag1 = 0, disp = 0;
	i = 0;
	cout << "\n";
	while(str[i] != '\0'){

		//CODE TO IMPLEMENT 2 to 8 character logic
		if(flag == 1){

			// LOGIC TO CHECK WHETHER THE GIVEN CHARACTER IS A APLHABET OR NOT
			int ascii = (int)str[i];
			if ( (ascii >= 65 && ascii <= 90) || (ascii >= 97 && ascii <=122) ){
				count++;
			}

				// LOGIC TO CHECK WHETHER THE GIVEN CHARACTER IS ATLEAT 2 and NOT MORE THAN 8
			if(count >= 2){

				if(count <=8){
					flag = 1;
					flag1 = 1;
				}else{
					flag = 0;
					disp = 0;
					break;
				}

			}

		}

		// CODE TO CHECK WHETHER IT IS FOLOWED BY FOUR NUMBERS OR NOT

		if(flag == 1 && flag1 == 1){
			int numAscii = (int)str[i];
			if(numAscii >= 48 && numAscii <= 57 ){
				count1++;
			}
			// MORE THAN 4 NUMBERS  
			if(count1 > 4){
				disp = 0;
				break;
			}else if(count1 < 4){
				disp = 0;
			}else if(count == 4){
				if(str[i++] != '\0'){
					disp = 0;
					break;
				}else{
					disp = 1;
				}
			}
		}
		i++;
	}

	if(disp == 1){
		cout << "Valid String";
	}else{
		cout << "Invalid String";
	}

}